import { makeStyles } from "@material-ui/core";

export default makeStyles(() => ({
    title: {
        paddingLeft: '15vw',
        paddingBottom: '5vh',
        fontWeight: 'bold'
    },
    gridContainer: {
        padding: '0 5vw 5vh'
    }
}))